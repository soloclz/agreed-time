# Production environment example
# Copy this to .env in the deploy directory

# Database Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=secure_password_here
POSTGRES_DB=agreed_time

# Backend Configuration
# The domain where your frontend is hosted
ALLOWED_ORIGINS=https://agreedtime.com

# Frontend Configuration
# The API base URL that the frontend will use (injected at build time)
PUBLIC_API_BASE_URL=https://api.agreedtime.com
